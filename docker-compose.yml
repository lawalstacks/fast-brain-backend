version: '3.8'
services:  
  app:    
    build:
      context: .
      target: development    
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - NODE_ENV=${NODE_ENV}
      - PORT=${PORT}
    volumes:
      - .:/app
      - /app/node_modules